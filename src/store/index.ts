import Vue from 'vue';
import Vuex from 'vuex';

import Counter1 from '@/store/Counter1';

Vue.use(Vuex);

export default new Vuex.Store({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    Counter1,
  },
});
